<plan>
  <phase>1</phase>
  <plan-number>01</plan-number>
  <type>execute</type>
  <name>Bootstrap T3 App</name>
</plan>

<objective>
Scaffold the T3 stack application using create-t3-app with Next.js App Router, tRPC, Prisma, Tailwind CSS, and NextAuth.js. Install shadcn/ui. Create .env.example with all required environment variables documented. Verify dev server starts and build type-checks cleanly.

Traces to: PRD §4 Stack, PRD §4 Architecture
</objective>

<context>
- PRD: .planning/specs/PRD.md
- Project: .planning/PROJECT.md
- Discovery: .planning/phases/01-scaffold/DISCOVERY.md
</context>

<tasks>
  <task type="auto">
    <name>Scaffold T3 app with create-t3-app</name>
    <files>All files created by create-t3-app in /Users/oliver/repos/financemanager/</files>
    <action>
      Run create-t3-app in the existing directory. Use non-interactive mode with all required flags:

      cd /Users/oliver/repos/financemanager
      npx create-t3-app@latest . --CI --trpc --prisma --tailwind --nextAuth --appRouter --noGit --importAlias "@/*"

      Note: The directory already has a README.md — create-t3-app should populate into the existing dir.
      If the CLI prompts for confirmation to use existing directory, confirm yes.
    </action>
    <verify>
      - [ ] package.json exists with next, @trpc/server, prisma, tailwindcss, next-auth
      - [ ] src/app/layout.tsx exists
      - [ ] src/server/db.ts exists
      - [ ] src/server/api/trpc.ts exists
      - [ ] prisma/schema.prisma exists
    </verify>
    <done>T3 scaffold created with all required packages</done>
  </task>

  <task type="auto">
    <name>Install shadcn/ui</name>
    <files>components.json, src/components/ui/</files>
    <action>
      cd /Users/oliver/repos/financemanager
      npx shadcn@latest init --defaults

      Then install commonly used components for this project:
      npx shadcn@latest add button card badge select dropdown-menu table tabs progress dialog

      This installs pre-built accessible components styled with Tailwind.
    </action>
    <verify>
      - [ ] components.json exists at project root
      - [ ] src/components/ui/button.tsx exists
      - [ ] src/components/ui/card.tsx exists
      - [ ] src/components/ui/table.tsx exists
      - [ ] src/lib/utils.ts exists (cn helper)
    </verify>
    <done>shadcn/ui initialised with core components installed</done>
  </task>

  <task type="auto">
    <name>Install additional dependencies</name>
    <files>package.json</files>
    <action>
      cd /Users/oliver/repos/financemanager
      npm install recharts date-fns

      recharts — chart library for Phase 9
      date-fns — date formatting utilities used throughout
    </action>
    <verify>
      - [ ] recharts in package.json dependencies
      - [ ] date-fns in package.json dependencies
    </verify>
    <done>Additional deps installed</done>
  </task>

  <task type="auto">
    <name>Create .env.example</name>
    <files>.env.example</files>
    <action>
      Create .env.example documenting all required environment variables:

      ```
      # NextAuth
      NEXTAUTH_URL="http://localhost:3000"
      NEXTAUTH_SECRET=""  # Generate with: openssl rand -base64 32

      # Google OAuth (https://console.cloud.google.com)
      AUTH_GOOGLE_ID=""
      AUTH_GOOGLE_SECRET=""

      # Neon PostgreSQL (https://neon.tech)
      DATABASE_URL=""  # Format: postgresql://user:pass@host/dbname?sslmode=require

      # Basiq API (https://dashboard.basiq.io)
      BASIQ_API_KEY=""
      BASIQ_WEBHOOK_SECRET=""  # Set in Basiq dashboard webhook config
      ```

      Also update src/env.js to include BASIQ_API_KEY and BASIQ_WEBHOOK_SECRET in the server schema.

      Copy .env.example to .env (the actual .env will have real values added manually by the user).
      Do NOT put real values in .env.example or commit real values.
    </action>
    <verify>
      - [ ] .env.example exists with all 6 variables documented
      - [ ] .env exists (as copy of .env.example, no real values)
      - [ ] src/env.js includes BASIQ_API_KEY and BASIQ_WEBHOOK_SECRET in server schema
    </verify>
    <done>.env.example created, src/env.js updated</done>
  </task>

  <task type="auto">
    <name>Configure auth middleware to protect all routes</name>
    <files>src/middleware.ts</files>
    <action>
      Create or update src/middleware.ts to protect all routes except /login and /api/auth and /api/webhooks/basiq.

      ```typescript
      export { auth as middleware } from "@/server/auth";

      export const config = {
        matcher: ["/((?!api/auth|api/webhooks|_next/static|_next/image|favicon.ico|login).*)"],
      };
      ```

      This ensures every page route redirects to /login if unauthenticated.
      The /api/webhooks/basiq route is excluded since it uses its own HMAC signature verification.
    </action>
    <verify>
      - [ ] src/middleware.ts exports auth as middleware
      - [ ] matcher excludes /login, /api/auth, /api/webhooks, static assets
    </verify>
    <done>Middleware configured</done>
  </task>

  <task type="auto">
    <name>Update root page to redirect</name>
    <files>src/app/page.tsx</files>
    <action>
      Update src/app/page.tsx to redirect authenticated users to /dashboard and unauthenticated to /login:

      ```typescript
      import { redirect } from "next/navigation";
      import { auth } from "@/server/auth";

      export default async function Home() {
        const session = await auth();
        if (session) {
          redirect("/dashboard");
        } else {
          redirect("/login");
        }
      }
      ```

      Create placeholder pages for routes that will be built in later phases:
      - src/app/dashboard/page.tsx — "Dashboard coming in Phase 7"
      - src/app/login/page.tsx — Basic login page with NextAuth signIn button
      - src/app/transactions/page.tsx — "Transactions coming in Phase 6"
      - src/app/budgets/page.tsx — "Budgets coming in Phase 8"
      - src/app/analytics/page.tsx — "Analytics coming in Phase 9"
      - src/app/settings/accounts/page.tsx — "Account settings coming in Phase 4"
    </action>
    <verify>
      - [ ] src/app/page.tsx redirects based on session
      - [ ] All placeholder pages exist
      - [ ] /login page has a sign-in button using NextAuth
    </verify>
    <done>Root redirect and placeholder pages created</done>
  </task>

  <task type="auto">
    <name>Verify build passes</name>
    <files>N/A (verification only)</files>
    <action>
      cd /Users/oliver/repos/financemanager

      First install all deps: npm install
      Then type-check: npm run typecheck (or npx tsc --noEmit)

      Note: DATABASE_URL and other env vars may not be set yet — this is acceptable.
      The T3 env.js will need DATABASE_URL to not be empty. Temporarily set:
      DATABASE_URL="postgresql://placeholder:placeholder@localhost/placeholder"
      in .env to allow type check to pass.

      Run: npm run build 2>&1 | tail -20
      Expected: Build succeeds or only fails due to missing real env var values (not type errors).
    </action>
    <verify>
      - [ ] npm run typecheck passes with 0 TypeScript errors
      - [ ] No missing import errors
    </verify>
    <done>Type check clean</done>
  </task>
</tasks>

<verification>
  - [ ] create-t3-app scaffold complete with tRPC, Prisma, Tailwind, NextAuth
  - [ ] shadcn/ui installed with button, card, badge, select, dropdown-menu, table, tabs, progress, dialog
  - [ ] recharts and date-fns installed
  - [ ] .env.example documents all 6 required variables
  - [ ] src/env.js includes BASIQ_API_KEY and BASIQ_WEBHOOK_SECRET
  - [ ] Middleware protects all routes (except /login, /api/auth, /api/webhooks, static)
  - [ ] Placeholder pages exist for all routes
  - [ ] TypeScript type check passes
</verification>

<success_criteria>
Running `npm run typecheck` exits with 0 errors. `npm run dev` starts the development server. The root page redirects to /login. All placeholder routes exist.
</success_criteria>
