# Phase 3 Plan 01: Authentication (NextAuth + Google)

## Goal
Fix the two critical auth bugs from Phase 1 scaffold: (1) missing custom sign-in page config,
(2) Prisma imported in edge middleware. Implement the split auth config pattern.
Verify PRD US-001 acceptance criteria.

## CoVe Required
This plan touches auth/session logic — apply 4-stage CoVe verification.

## Tasks

### Task 1: Fix `src/server/auth/config.ts` — make edge-safe
Remove all Prisma/DB imports. Add `pages.signIn`. Add `authorized` callback.

**Verify:**
- [ ] No `@auth/prisma-adapter` import
- [ ] No `@/server/db` import
- [ ] `pages: { signIn: '/login' }` present
- [ ] `authorized` callback returns `!!auth?.user`

### Task 2: Fix `src/server/auth/index.ts` — own the Prisma adapter
Move PrismaAdapter here. Add session callback to inject user.id.

**Verify:**
- [ ] `PrismaAdapter(db)` in NextAuth config
- [ ] `session` callback injects `user.id`
- [ ] Spreads `authConfig` from config.ts
- [ ] `auth` is still wrapped with React `cache`

### Task 3: Fix `src/middleware.ts` — use edge-safe auth only
Create a separate NextAuth instance from `authConfig` only (no Prisma).

**Verify:**
- [ ] Does NOT import from `@/server/auth/index.ts`
- [ ] Imports `authConfig` from `@/server/auth/config`
- [ ] Matcher excludes `/login`, `/api/auth`, `/api/webhooks`

### Task 4: Typecheck
```bash
npm run typecheck
```

**Verify:**
- [ ] 0 TypeScript errors

## CoVe Verification (4-stage)

### Stage 1 — Draft
Implement all three file changes as described.

### Stage 2 — Verification Questions
1. Can an unauthenticated user access `/dashboard`?
2. Does unauthenticated redirect go to `/login` (not `/api/auth/signin`)?
3. Is there a redirect loop between `/login` and middleware?
4. Does `user.id` appear in `ctx.session.user.id` in tRPC?
5. Will middleware import chain pull in Prisma (edge failure)?

### Stage 3 — Answers
1. NO — `authorized` returns `!!auth?.user` = false → redirect
2. YES — `pages: { signIn: '/login' }` configures the redirect target
3. NO — middleware matcher excludes `/login` path, so `/login` is never checked
4. YES — `session` callback does `id: user.id` where `user` is the DB User
5. NO — middleware imports only from `@/server/auth/config` which has no Prisma imports

### Stage 4 — Verified Implementation
All answers confirmed safe. Proceed with implementation.

## Plan Verification
- `src/server/auth/config.ts` has NO Prisma imports, HAS pages.signIn, HAS authorized callback
- `src/server/auth/index.ts` HAS PrismaAdapter, HAS session callback with user.id
- `src/middleware.ts` imports from `@/server/auth/config` (not index)
- `npm run typecheck` exits 0
- Auth flow: unauthed → /login; authed → proceeds
