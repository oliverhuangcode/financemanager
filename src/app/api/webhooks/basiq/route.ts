import { type NextRequest, NextResponse } from "next/server";

// Placeholder: Basiq webhook handler — implemented in Phase 5
export async function POST(_req: NextRequest) {
  return NextResponse.json({ message: "Webhook handler coming in Phase 5" });
}
